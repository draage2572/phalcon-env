# nginx-dockerfile
FROM phalconphp/nginx

#config
ADD config/default.conf /etc/nginx/conf.d
ADD config/phalcon-boot.conf /etc/nginx/sites-enabled
ADD config/phalcon-bootstrap.conf /etc/nginx/sites-enabled
